# 更新日志

## 未发布（v1.0.1 待发）

### 新增

- **免费体验 + 终身会员（应用内购买）**：免费体验 5 座市州级行政区，点亮第 6 座时弹出购买面板，
  走系统收银台完成 **¥6.00 一次性买断**（非消耗型商品 `travelchina_lifetime`）。
  实现依据华为官方《接入购买》《权益发放》《IAP Kit 接入规范》：
  `queryEnvironmentStatus → queryProducts → createPurchase → 解码 jwsPurchaseOrder → 发放权益 → finishPurchase`；
  启动时按官方要求查询一次已购非消耗型商品，覆盖卸载重装与换机场景；
  购买面板不展示任何支付方式名称 / logo，价格一律使用 AGC 返回的本地化价格。
  老用户已点亮的城市不回滚，只对"新增点亮"生效。接入步骤见 `docs/应用内购买接入指南.md`。
- **华为账号一键登录**：用 Account Kit 官方组件 `LoginWithHuaweiIDButton`（自带华为账号授权页与
  用户协议勾选），登录成功后只在本机保存 `openID` / `unionID`，用于将来区分云端数据归属；
  不保存 authorizationCode / idToken 等临时凭据。未登录也能完整使用全部本地功能。
- **点亮颜色 8 选 1**（我的 → 点亮颜色）：纯黄 / 暖金 / 纯红 / 橙红 / 青绿 / 天蓝 / 紫 / 玫红。
  点位用预烘的 8 套 PNG 图标，城市名自动换成同色系深色版（保证压在浅色底图上仍然清晰），选择结果会记住。

### 修复

- **点名字就能点亮**：自绘的地名标签虽然外层设了 `HitTestMode.None`，但内部的 Text 仍会被命中，
  点在名字上时事件被它吃掉 → 整棵标签子树改为不参与命中，点名字与拖动地图可以同时成立。
- **点按更灵敏**：新增自己的点按识别（按下-抬起位移 <16vp、时长 <500ms 即判定为点击），
  绕开"手指微抖被地图当成拖拽因而不回调 `mapClick`"的问题；命中半径 44 → 56vp。
- **城市卡片右上角重叠**：`SheetSize.MEDIUM` 会画系统关闭按钮(X)，给标题行加右内边距让开。

### 优化

- **云同步入口文案**：云同步尚未开放时显示"云同步即将开放，敬请期待"，
  不再把 AGC 控制台的操作步骤暴露给普通用户（细节只写 hilog）。
- **10 种徽章点亮动画**（可切换、可试播）：勋章升起 / 抛硬币 / 心跳脉冲 / 摇铃 / 爆闪 /
  穿越 / 蓄力弹出 / 转盘 / 呼吸浮现 / 天降。
  在「我的 → 成就动画」里点「换一个」逐个切换（切换即试播），
  「重播一次」反复看；选择结果会记住，统计页解锁新徽章时用的就是它。
- **已点亮点位多频率闪烁**：地图上所有已点亮城市的光点会持续呼吸式闪烁，
  周期（0.9~1.9 秒）与相位由行政区划代码派生，每座城市都不一样，
  因此永远不会同步 —— 一眼就能看出去过哪些地方。闪烁只在页面可见时运行。
- **页签切换动画**：`animationMode(CONTENT_FIRST_WITH_JUMP)` + 弹簧曲线 + 320ms 时长。
  默认动画按页签序号差横向滑动，从地图(0) 跳到我的(3) 时内容要从三屏外飞进来，非常生硬。
- **页面入场动效**：清单 / 统计 / 我的首次进入时淡入上浮；地图的悬浮胶囊与图例淡入下滑。
- **成就徽章点亮动画（粒子级）**：新解锁的徽章会**散成 18 颗粒子**飞向屏幕中心，
  汇聚成**一枚大勋章**点亮闪耀（光晕呼吸两次 + 星芒四射），再缩小飞回原来的卡片位置；
  用 preferences 记录"已庆祝过"的徽章，保证每枚只庆祝一次；
  未解锁徽章改用灰度滤镜 + 降透明度，已解锁徽章常驻柔和金色光晕。
- **沉浸光感上的文字**：顶部胶囊、图例、底部页签栏未选中项的文字全部改为纯白
  （主文字色 `#EAF0FA` → `#FFFFFF`）。

## v1.0.0 - 2026-09-14

首个正式版本（versionCode 1000000）。

### 功能

- 全国 **370 个市州级行政区**点位地图：去过的城市点亮成金色，没去过的保持暗色。
- 点按地图点亮城市：按**屏幕距离**（44vp，约一个指尖）判定最近城市，全国视野下也能一点即中，不再要求"正好点在圆点上"。
- 分级地图标注：全国视野显示省份名，放大显示省会，再放大铺开全部地级市名；字号、字重、描边与底图标签保持一致，并做标签碰撞避让。
- 足迹清单：按省分组、搜索、批量点亮、已点亮/未点亮筛选。
- 统计与成就：覆盖率环形图、到访最多的城市、**9 枚成就徽章**。
- 数据导出：一键导出 JSON。
- 华为账号登录：用 Account Kit 官方一键登录组件接入（云端同步见下方"未发布"计划）。
- 「我的」页面：大字号模式、色盲友好模式、清除本机数据、隐私政策全文。

### HarmonyOS 7 适配

- 底部页签栏改用系统 **Tabs 悬浮页签栏**（`barFloatingStyle` + `uiMaterial` 沉浸光感）：`barOverlap(true)` + `vertical(false)` + `barPosition(BarPosition.End)` 三条件同时满足，材质才会被系统渲染；鸿蒙 6 及更早设备自动退回"整条页签栏 + 毛玻璃"。
- 地图上的悬浮控件（顶部胶囊、图例、右侧圆按钮）改用真实 `backgroundBlurStyle`：官方明确这类组件不在沉浸光感生效范围内，硬套 `systemMaterial` 只会得到 `Material inactive: out of scope` 且界面上毫无变化。
- 「我的 → 关于」新增沉浸光感诊断信息（API 版本 / 算力档位 / 应用开关 / filter）。

### 合规

- 新增**首次启动隐私政策同意弹层**：不同意则退出应用；同意状态落盘，政策版本升级会重新询问。
- 新增**应用内隐私政策全文**（我的 → 关于 → 隐私政策）。
- 新增**清除本机足迹数据**（带二次确认）。
- 补全权限用途说明；不申请后台定位。

### 修复

- 修复底部玻璃"半透明但不模糊"的问题（根因：`systemMaterial` 用在了生效范围之外）。
- 修复点按城市需要反复点击的问题（公里阈值改为屏幕距离阈值）。
- 修复应用内版本号与构建配置不一致的问题。
